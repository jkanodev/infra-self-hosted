name: Build and Deploy

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/jkanodev/infra-self-hosted

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify image in registry
        run: |
          echo "Verifying image was pushed to GHCR..."
          docker pull ${{ env.IMAGE_NAME }}:latest
          docker image inspect ${{ env.IMAGE_NAME }}:latest --format 'Pushed: {{.Id}}'
          echo "Push verified. Image is available."

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: success()

    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            echo "Pulling latest image..."
            docker pull ${{ env.IMAGE_NAME }}:latest
            docker stop portfolio-web 2>/dev/null || true
            docker rm portfolio-web 2>/dev/null || true
            docker run -d --name portfolio-web --restart unless-stopped -p 80:80 ${{ env.IMAGE_NAME }}:latest
            echo "Deploy complete."
            docker ps
